import React from 'react';

const UndoRedo = () => {
    return (
        <div className="undo-redo">
            <button>Undo</button>
            <button>Redo</button>
        </div>
    );
};

export default UndoRedo;
